import React, { Component, } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

function Me() {
	return (
		<React.Fragment>
			Hello World
		</React.Fragment>
	);
}

Me.propTypes = propTypes;

export default Me;